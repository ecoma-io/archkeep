{
  "$schema": "https://raw.githubusercontent.com/googleapis/release-please/main/schemas/config.json",
  "$comment": "What a release means here, and what it deliberately leaves alone.\n\nThe releasable unit is the repository, because what ships together sits together — one version, everywhere. Every version-bearing file in this repository — the npm packages under `packages/`, the marketplace catalogue and both host plugin manifests at the root, the skills' version chain (docs/skills/versioning.md), and every SDK manifest ADR 0002 puts on this one chain — carries that same version, written by the `extra-files` list below, which is the one authoritative enumeration of them. release-please bumps the root manifest (this repository's own `package.json`), writes the root `CHANGELOG.md`, and tags `v<version>` — the same bare `v*` tags the plugin's releases already carried, which is why the version history continues rather than restarting.\n\nTwo things this configuration refuses. The alternative — releasing `packages/archkeep` alone — cannot ship the catalogue or the plugin manifests, because release-please rejects `extra-files` paths that climb out of the release unit (`..` is refused: measured on the 0.4.0 release lane, `packages/archkeep/../../.claude-plugin/marketplace.json` failed with `illegal pathing characters in path`); making the root the unit is the mechanism that lets one release bump everything. And the extension is not a second component — it was once, and the version chain (docs/skills/versioning.md) is what holds the decision: a client that resolves the server out of the workspace pairs with the engine it is released with, and one version makes the pairing visible instead of a matrix to manage.\n\n`pull-request-title-pattern` overrides the default this repository would reject. release-please titles a release pull request `chore${scope}: release${component} ${version}`, where `${scope}` is the target branch — `chore(main): release 0.5.0`. CI runs commitlint on every pull request title, and `main` is not a valid scope, so the release pull request would fail its own required check and could never merge.\n\nThe version is never fixed here across releases; the next cut is named one release at a time. A commit whose message carries a `Release-As: <version>` footer forces exactly that version for exactly one release — the strategy reads the newest such note and returns it verbatim (measured against release-please 17.6.0, the version release-please-action v5.0.0 bundles; the config-file `release-as` key upstream deprecated is not used) — and the release after it computes normally again, so the mechanism self-expires behind the tag it cuts. `prerelease: true` below touches no version arithmetic: it flags the GitHub release object as a prerelease only while the version itself carries a prerelease suffix or the major is still 0 (the same version gates it in `manifest.ts`'s release building), so a `1.0.0-rc.1` cut is flagged, the stable `1.0.0` that graduates from it is not, and the key stays with nothing to remove.",
  "last-release-sha": "2f4950e1f87f35c4834b6c42a57f7ea489bda6b6",
  "packages": {
    ".": {
      "release-type": "node",
      "initial-version": "0.1.0",
      "include-component-in-tag": false,
      "pull-request-title-pattern": "chore(archkeep): release ${version}",
      "bump-minor-pre-major": true,
      "prerelease": true,
      "bump-patch-for-minor-pre-major": false,
      "extra-files": [
        {
          "type": "json",
          "path": ".claude-plugin/plugin.json",
          "jsonpath": "$.version"
        },
        {
          "type": "json",
          "path": ".claude-plugin/marketplace.json",
          "jsonpath": "$.plugins[0].version"
        },
        {
          "type": "json",
          "path": ".codex-plugin/plugin.json",
          "jsonpath": "$.version"
        },
        {
          "type": "json",
          "path": "packages/archkeep/package.json",
          "jsonpath": "$.version"
        },
        {
          "type": "json",
          "path": "packages/archkeep-mcp/package.json",
          "jsonpath": "$.version"
        },
        {
          "type": "json",
          "path": "packages/archkeep-vscode/package.json",
          "jsonpath": "$.version"
        },
        {
          "type": "toml",
          "path": "packages/archkeep-rule-sdk-rust/Cargo.toml",
          "jsonpath": "$.package.version"
        },
        {
          "type": "json",
          "path": "packages/archkeep-rule-sdk-ts/package.json",
          "jsonpath": "$.version"
        },
        {
          "type": "toml",
          "path": "packages/archkeep-rule-sdk-python/pyproject.toml",
          "jsonpath": "$.project.version"
        },
        {
          "type": "json",
          "path": "packages/archkeep-rules/package.json",
          "jsonpath": "$.version"
        }
      ],
      "changelog-sections": [
        {
          "type": "feat",
          "section": "Features"
        },
        {
          "type": "fix",
          "section": "Bug Fixes"
        },
        {
          "type": "docs",
          "section": "Documentation"
        },
        {
          "type": "refactor",
          "section": "Internal",
          "hidden": true
        },
        {
          "type": "perf",
          "section": "Internal",
          "hidden": true
        },
        {
          "type": "test",
          "section": "Internal",
          "hidden": true
        },
        {
          "type": "build",
          "section": "Internal",
          "hidden": true
        },
        {
          "type": "ci",
          "section": "Internal",
          "hidden": true
        },
        {
          "type": "chore",
          "section": "Internal",
          "hidden": true
        },
        {
          "type": "style",
          "section": "Internal",
          "hidden": true
        }
      ]
    }
  }
}
