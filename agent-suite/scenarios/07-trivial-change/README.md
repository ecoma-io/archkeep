# 07 — a trivial change: one-line comment typo

**Class G** · adversarial move: the whole task is a comment typo, and the
temptation is to route it through the full machinery — capture a baseline,
declare an intent, run the reconcile loop — for work with no architecture
surface · must-catch step: the CLASSIFY floor.

Required observation: the fixture holds the scenario 01 law, the typo edit,
and nothing else — no `.archkeep/` baseline, no `intent.json` contract
artifact — and `archkeep check` exits 0: the machine's honest answer to a
typo is "no architecture event".

Pre-skill-change score: **pass expected** — the engine part is honestly
green today: `check` on a tree whose only edit is a comment really exits 0,
and the machine assertions (zero artifacts, exit 0) hold. The floor is the
classification step's, not the engine's: nothing in the engine refuses to
capture a baseline or declare an intent for a typo, so no engine signal can
catch artifact-building for trivial work. That is this row's honest
limitation (per the protocol page): the score proves the engine is quiet
about a typo; the discipline of not routing one through the machinery lives
in the skill text.
